<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tigcal.utils">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.BillCalc">
        <activity
            android:name=".BillCalcActivity"
            android:icon="@mipmap/ic_bc_launcher"
            android:label="@string/app_bill_calc"
            android:roundIcon="@mipmap/ic_bc_launcher_round">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BatteryActivity"
            android:icon="@mipmap/ic_b_launcher"
            android:label="@string/app_battery"
            android:roundIcon="@mipmap/ic_b_launcher_round"
            android:theme="@style/AppTheme.BillCalc">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>